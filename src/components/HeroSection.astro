---
import Logo from '../assets/Logo.jpeg';
import Fondo from '../assets/fondo.jpeg';
---

<section class="relative h-screen flex items-center justify-center bg-cover bg-center" style={`background-image: linear-gradient(rgba(0,0,50,0.7), rgba(0,0,0,0.8)), url(${Fondo.src});`}>
	<div class="text-center text-white px-4 md:px-8 max-w-4xl">
		<img src={Logo.src} alt="Logo Stellar" class="mx-auto mb-6 w-32 h-32 md:w-40 md:h-40 rounded-full shadow-2xl animate-pulse" />
		<h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6 animate-fade-in">
			¡Bienvenidos a Stellar: Misión Espacial! 🚀
		</h1>
		<p class="text-xl md:text-2xl lg:text-3xl mb-8 font-light">
			Prepárate para despegar hacia una aventura fuera de este mundo
		</p>
		<p class="text-lg md:text-xl mb-10 max-w-2xl mx-auto">
			Donde descubriremos que Dios nos ve, nos ama y nos envía con una misión muy especial
		</p>
	
		
		<a href="https://forms.gle/e5Xi8hRgzik8xU8d6" target="_blank" rel="noopener noreferrer" 
		   class="inline-block bg-yellow-400 hover:bg-yellow-500 text-black font-bold py-4 px-8 md:px-12 rounded-full text-lg md:text-xl transition-all transform hover:scale-105 shadow-2xl">
			🛰️ ¡Inscríbete y Despega!
		</a>
	</div>
</section>

<style>
	@keyframes fade-in {
		from {
			opacity: 0;
			transform: translateY(20px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}
	
	.animate-fade-in {
		animation: fade-in 1s ease-out;
	}
</style>
